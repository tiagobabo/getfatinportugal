  <%= form_for [:admin_configuration, @service_modality], :html => {:class => 'custom',"data-abide" => ''} do |f| %>
	 <% if @service_modality.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@service_modality.errors.count, "error") %> prohibited this category from being saved:</h2>

	      <ul>
	      <% @service_modality.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

  <fieldset>
    <legend class="contact legend">Service_modalities...</legend>

    <div class="row">
      <div class="large-12 columns">
		 <%= f.label :name %><br />
		<%= f.text_field :name, :required => true, :placeholder=>"Name of the service modality..."%>		
		<small class="error">Name is required.</small>       
      </div>
    </div>
	

<%= f.submit 'submit', :class => 'small button' %>
   
  </fieldset>
</form>
<%end%>