  <%= form_for [:admin_configuration, @client_type], :html => {:class => 'custom',"data-abide" => ''} do |f| %>
	 <% if @client_type.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@client_type.errors.count, "error") %> prohibited this category from being saved:</h2>

	      <ul>
	      <% @client_type.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

  <fieldset>
    <legend class="contact legend">Client type...</legend>

    <div class="row">
      <div class="large-12 columns">
		 <%= f.label :name %><br />
		<%= f.text_field :name, :required => true, :placeholder=>"Name of the client type..."%>		
		<small class="error">Name is required.</small>       
      </div>
    </div>
	

<%= f.submit 'submit', :class => 'small button' %>
   
  </fieldset>
</form>
<%end%>