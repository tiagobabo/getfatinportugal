<% @products.each do |product| %>

  <!-- Feed Entry -->
  <div class="row">
    <div class="large-2 columns small-3">
	<a href ="<%= product_path(product) %>">
	<img src="<%= product.photo %>" />
	</a>
	<p> <%= product.name%> </p>
	</div>
    <div class="large-10 columns">
	<p> <strong>Region: </strong> <%=product.region %> </p>
	<p> <strong>Category: </strong> <%=@category_name %> </p>
      <p> <strong>Description: </strong> <%=product.description %> </p>	
	  <div class="map-canvas" id="<%= product.id%>"></div>
	  <script>
	  googleMaps.mapCanvas = "<%= product.id%>";
 googleMaps.initializeCoordinates(<%=product.latitude %>, <%= product.longitude %>, false);
</script>
	  <p> <strong>Coordinates: </strong> latitude: <%=product.latitude %> -  longitude: <%= product.longitude %> </p>	     
    </div>
	<hr />
  </div>
  <!-- End Feed Entry -->

  
  
<% end %>